# HANDOFF

## Day 1 — 2026-09-23 — Mohammed (solo, both tracks)
Built: Engine (FastAPI) with `/health` (verified 200 live), `POST /projects`, `GET /projects/:id/context`, and full tasks CRUD wired to SQLAlchemy models mirroring the frozen schema — routes compile + register; live-DB test pending Supabase creds. OpenCode fork source vendored at `opencode-plugin/opencode/`, runs from source via bun (`bun run --cwd packages/opencode src/index.ts`); visible rebrand done — SCAFFOLD banner in `packages/opencode/src/cli/ui.ts`, terminal titles ("Scaffold" / "SC | <session>") in `packages/tui/src/app.tsx`. Plugin skeleton at `opencode-plugin/.opencode/plugins/scaffold.ts` (log-only hooks: `session.created`, `tool.execute.before/after`, `shell.env`). Dashboard (Vite+React+TS) builds clean with a project-join screen that calls `/context`. Frozen: `docs/SCHEMA.md` (10 tables + indexes), `docs/API_CONTRACTS.md` (routes, MCP tool names, env vars, verified OpenCode hook API).
Env vars added: `DATABASE_URL` (engine — Supabase pooler string; Day 1 addition to the frozen list), `SCAFFOLD_ENGINE_URL` (opencode-plugin — Day 1 addition). All documented in `docs/API_CONTRACTS.md`.
Still broken / not done: No Supabase project yet → schema.sql not executed against real Postgres, task CRUD untested against live DB (run `engine/app/db/schema.sql` in the Supabase SQL editor, fill `engine/.env` from `.env.example`, then `python -m app.scripts.seed` + curl the task routes). `bun install` needs `--ignore-scripts` on Windows (tree-sitter-powershell node-gyp fails; lazy feature, safe to skip). Fork identity rename deferred: binary/package names, `~/.config/opencode` paths, TTY graphic glyphs still OPENCODE (`packages/tui/src/logo.ts`). Deploy deferred to Day 2 by decision. Plugin hooks log only — no engine calls until Day 4. Nothing pushed to GitHub yet (remote set; auth pending).
Next pair should start with: Create the Supabase project + run schema.sql, then Day 2 scope — `/github-webhook` route with `diff_parser.py`, and `mcp_server.py` with the 6 frozen tool names; keep the engine's deterministic-vs-LLM split (repo rules 2-3).
